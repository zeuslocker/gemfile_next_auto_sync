# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'catalogue'
gem 'Generator_pdf'
plugin 'gemfile_next_auto_sync', git: 'git@github.com:zeuslocker/gemfile_next_auto_sync.git'
